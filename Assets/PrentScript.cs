using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrentScript : MonoBehaviour
{
    public float fieldStrength = 1.0f; // Strength of the field
    public float fieldRange = 5.0f; // Range of the field
    public float fieldFrequency = 1.0f; // Frequency of the field
    public float fieldPhase = 0.0f; // Phase of the field

    private Material fieldMaterial; // Material used to render the field

}